# Aegis_AI
Aegis AI is a RAG-powered autonomous debugging system for ML and LLM pipelines. It analyzes logs, classifies failures (data, model, prompt, retrieval), performs root cause analysis using LLMs, and suggests or applies fixes to maintain system performance in real-world environments.
