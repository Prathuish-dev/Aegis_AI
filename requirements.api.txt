# ============================================================
# Aegis AI — API Container Requirements (lean, no torch/ML)
# Used by the FastAPI Docker image to keep the image small.
# Heavy ML packages (sentence-transformers, torch, ragas)
# are only needed when re-ingesting the knowledge base, not
# for the API + healing engine at runtime.
# ============================================================

# Core LLM & Agent
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0
langsmith>=0.1.0

# Vector DB (ChromaDB uses its own embeddings client)
chromadb>=0.5.0

# Statistical / detection
scipy>=1.10.0
evidently>=0.4.0
numpy>=1.26.0
pandas>=2.2.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
rich>=13.0.0
httpx>=0.27.0
pyyaml>=6.0
